_targets
_targets/
## per-district stores (see _targets.yaml); the trailing glob keeps
## _targets_dataprep.R and _targets_omniscape.R tracked
_targets_dataprep_*
_targets_omniscape_*
Data
Data/
Outputs
Outputs/
README_files/
Teams
Teams/
*.html
.ipynb_checkpoints
*/.ipynb_checkpoints/*
.Rproj.user
_tmp.R
_tmp/

# One-off corrections report: the Rmd is tracked, its rendered output and figures are not.
reports/*.pdf
reports/figures/
## knitr/quarto build artefacts: `<name>_files/` holds the figures a render extracts, and a
## `quarto preview` recreates it on every pass.
reports/*_files/

# stray device output from a test that accidentally called graphics::box()
Rplots.pdf
